name: volteras_analytics
version: "1.0.0"
config-version: 2

profile: volteras_analytics
model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  volteras_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table

